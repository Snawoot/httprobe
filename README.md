# httprobe
Simple tool to test HTTP requests

## Synopsys

```
httprobe <URL>

Options:
  -method string
    	HTTP method (default "GET")
  -proxy string
    	proxy URL
  -timeout duration
    	operation timeout (default 5s)
```
